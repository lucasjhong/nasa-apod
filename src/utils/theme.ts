import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		mode: 'dark',
		primary: {
			main: '#9CC9E3',
		},
		secondary: {
			main: '#9cc9e3',
		},
	},
});

export default theme;
